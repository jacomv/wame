# Admin API key — has full access to all instances (optional if using accounts only)
API_KEY=change-this-to-a-strong-secret-key

# Optional — defaults shown
PORT=3000
SESSION_DIR=./data/sessions
DATA_DIR=./data
RATE_LIMIT=100
SEND_RATE_LIMIT=30
CORS_ORIGIN=*
